# S1: deletion-minimality (PR #228)

## Procedures and properties in design-pre absent from design-post

- **`appliedKeys` slice as primary tracking shape** (P1 pre, P2 pre). Classification: **(c) relocated to types**. The slice-with-duplicates becomes a map-with-uniqueness; the last-writer-wins invariant is encoded by the map shape rather than enforced by hand.
- **"Same node can contribute keys twice" property** (P2 pre). Classification: **(d) redundant** with the structural invariant the new map encodes. The pre-state design had to document the duplication as a property; post does not need to.

## Procedures and properties in design-post absent from design-pre

- **Per-node tracking with map semantics** (P1 post, P2 post). Classification: **post (a)**. Preferences require deterministic prune behavior; pre's design left the duplication accumulator as a runtime property the prune step had to handle correctly. Post encodes the constraint structurally.

## Verdict

**S1 improved.** One (c) relocation (slice → map shape encoding the invariant) and one (d) redundancy (pre-state property absorbed by post-state structure). One post-(a) addition for a constraint the preferences require.

Note: this is a bug fix the team labeled as such (`reconcile: fix forEach prune flake`). The design-level move that fixes the bug *is* a simplicity move under S1 (relocation to types). S1 reports `improved` because the metric scopes to the structural change, not the team's labeling. This is honest application of the prompt; it is also the conflation case worth flagging — bug fixes that take this shape will score as simplicity improvements regardless of label.

Citation: slice → map relocation; redundancy of property absorbed.
