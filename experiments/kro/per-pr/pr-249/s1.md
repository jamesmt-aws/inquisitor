# S1: deletion-minimality (PR #249)

## Procedures and properties in design-pre absent from design-post

- **GraphReconciler-as-monolith** (P1). Classification: **(c) relocated to module structure**. Twenty-five methods split between two receivers; concern split now legible.
- **Four-parameter threading** (P2). Classification: **(c) relocated to types**. `reconcileScope` struct encodes the per-reconcile context.
- **Teardown as separate procedure** (P3). Classification: **(b) absorbed** into prune. Design 005's claim moves from prose to code.
- **graphcache, hash, metrics files** (P4). Classification: **(a) deleted**. Same logic as #245/#247: input cases not in preference-rewarded partition; deferred by holding pen.
- **CEL env registration repeated 4×** (P5). Classification: **(b) absorbed** into one helper.
- **Separate ready/updated builders** (P6). Classification: **(b) absorbed** into parameterized factory.
- **scope.go, deletion.go standalone** (P7). Classification: **(c) relocated to module structure**.
- **gvkToGVR in controller** (P8). Classification: **(c) relocated to module structure** (resourcekey).
- **Two forEach state types** (P9). Classification: **(b) absorbed**.
- **nodeType parameter** (P10). Classification: **(d) redundant** with `node.Type()`.
- **11 exported condition symbols** (P11). Classification: **(c) relocated to module structure** (unexported).
- **Three namespace-defaulting copies** (P12). Classification: **(b) absorbed** into `defaultNamespace`.
- **Per-call-site SSA + status pattern** (P13). Classification: **(b) absorbed** into `ssaWrite`.

## Procedures and properties in design-post absent from design-pre

- **clusterAccess receiver** (P14). Classification: **post (a)**. Preferences require legible infrastructure dependencies; pre's monolith hid them.
- **reconcileScope type** (P15). Classification: **post (a)**. Type-level encoding of per-reconcile lifetime; pre threaded the same values implicitly.
- **`evalConditions` unifying three condition evaluations**. Classification: **post (a)**. Pre had three separate implementations of "evaluate CEL expressions and return bool"; post collapses them.

## Verdict

**S1 improved.** Most diverse mix of categories in the corpus alongside #253. Three (a) deletions for input cases the holding pen defers. Six (b) absorptions consolidating duplication. Five (c) relocations moving code to where it belongs. One (d) redundancy. Three post (a) additions encoding distinctions the preferences require.

Citation: this PR demonstrates every S1 category at least once. The sustained pattern is "implementation lags architecture; align them by structural moves."
