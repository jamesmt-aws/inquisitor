# S1: deletion-minimality (PR #203)

## Procedures and properties in design-pre absent from design-post

- **"Patch field release runs before template deletion" property** (P4 pre). Classification: **(c) relocated to ordering invariant**. The pre ordering is a property of the design that became wrong; post replaces it with the corrected ordering.
- **"No blocking mechanism for owner-side waits" property** (P3 pre). Classification: **(b) absorbed**. The negative-form property in pre becomes the positive composition (ownerReference + patch finalizer) in post.

## Procedures and properties in design-post absent from design-pre

- **`ownerDeleting()` detection.** Classification: **post (a)**. Preferences (correctness across owner-Graph teardown ordering) require this distinction.
- **`instanceLifecycle` patch node in stdlib.** Classification: **post (a)**. Preferences require the blocking mechanism for ordered teardown.
- **Patch-after-template ordering.** Classification: **post (a)**. Correctness requires this; pre ordering was wrong.
- **"No new API surface" property** (P5 post). Classification: **post (a)**. Public API stability is a stated preference; the change actively reinforces it by composing existing primitives.

## Verdict

**S1 unchanged.** Two pre-state items absorb into post-state structure (one ordering relocation, one negative-form absorption). Four post-(a) additions encode constraints the preferences require. No removable existing units in pre lost.

The "no new API" property is interesting: it is a constraint the change actively maintains rather than relaxes. S1's framing does not directly score this, but it is the kind of thing the doc's framing rewards (composition over new primitives).

Citation: one (c) ordering relocation; one (b) negative-form absorption; four post-(a) additions.
