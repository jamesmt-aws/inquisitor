# S2: partition match (PR #203)

## Input partition

- Graph teardown trigger: own delete vs owner Terminating.
- Teardown order: managed resources before lifecycle release vs after.
- Owner waits on Graph teardown / does not.

## Branching partition (pre)

- Trigger cell: own deletionTimestamp only.
- Teardown order cell: patch-release before template-delete.
- Blocking cell: not distinguished (owner does not wait).

## Branching partition (post)

- Trigger cells: own deletionTimestamp; owner Terminating (via `ownerDeleting()`).
- Teardown order cell: template-delete before patch-release.
- Blocking cell: owner blocks on finalizer placed by patch node.

## Cells in pre's branching partition not in post's

- **Wrong-order teardown cell.** Classification: **(b) deleted**. Input case (release before cleanup) is no longer reachable.

## Cells in post's branching partition not in pre's

- **Owner-Terminating trigger cell.** Classification: **(a) preferences require**. Correctness rewards Graph self-deleting when owner is Terminating.
- **Owner-blocking cell.** Classification: **(a) preferences require**. Correctness rewards owner waiting for managed-resource cleanup.

## Verdict

**S2 improved.** Pre's branching partition was coarser-than-required along two axes (no Owner-Terminating distinction; no blocking distinction). Post adds two preference-required cells and corrects an ordering. Same pattern as #224 — bug-fix-shaped change that registers as a partition-match improvement.

Citation: two preference-required cells added; one wrong-order cell deleted.
