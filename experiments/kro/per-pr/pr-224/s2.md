# S2: partition match (PR #224)

## Input partition

- Hard dependency vs readiness dependency: upstream change should trigger downstream re-eval in both cases.
- Resource scope: namespaced vs cluster-scoped.
- Readiness dispatch: deps already complete vs deps inflight.

## Branching partition (pre)

- Hard-dep cell: trigger fires.
- Readiness-dep cell: trigger does not fire (bug).
- Namespaced resource cell: routing matches.
- Cluster-scoped resource cell: routing mismatches (bug).
- Concurrent dispatch cell: not distinguished from sequential (bug).

## Branching partition (post)

- Hard-dep cell: trigger fires.
- Readiness-dep cell: trigger fires.
- Namespaced resource cell: routing matches.
- Cluster-scoped resource cell: routing matches with `namespace=""`.
- Concurrent dispatch cell: detected; trigger deposited for next reconcile.

## Cells in pre's branching partition not in post's

- None deleted.

## Cells in post's branching partition not in pre's

- **Readiness-dep propagation cell.** Classification: **(a) preferences require this distinction**. Correctness explicitly fails when this cell is missing.
- **Cluster-scoped routing cell.** Classification: **(a) preferences require**. Correctness fails when routing mismatches.
- **Concurrent dispatch detection cell.** Classification: **(a) preferences require**. Correctness rewards detecting and deferring stale dispatch.

## Verdict

**S2 improved.** Pre's branching partition was coarser-than-required along three axes (readiness-dep propagation, cluster-scoped routing, concurrent dispatch). Post adds three preference-required cells. This is exactly the doc's framing for "missing distinctions the preferences reward."

Worth noting in aggregator: S2 says `improved` on a bug fix that the team did not label as a simplification. The doc's framing supports this: bug fixes that take the form of "add the missing input-partition cell" *are* partition-match improvements regardless of how the team labels them.

Citation: three preference-required cells added to a previously-coarser branching partition.
