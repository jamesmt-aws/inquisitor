# S1: deletion-minimality (PR #224)

## Procedures and properties in design-pre absent from design-post

- None. Each pre-state property describes a missing distinction; post adds the distinction. No removable units in pre that go away in post.

## Procedures and properties in design-post absent from design-pre

- **ReadinessDependents propagation.** Classification: **post (a)**. Preferences (correctness) require this distinction; pre missed it.
- **Cluster-scoped namespace handling.** Classification: **post (a)**. Same.
- **Stale readiness dispatch detection.** Classification: **post (a)**. Same.
- **`DepositTrigger` API method.** Classification: **post (a)**. Required to support the deferred re-evaluation; preferences require correctness across concurrent dispatch.

## Verdict

**S1 unchanged.** No removable units in pre lost. Four post-(a) additions for input cases the preferences require but pre missed. S1 is the metric of removability; this change adds rather than removes, so the verdict is unchanged.

Citation: zero (a)/(b)/(c)/(d) removals; four post-(a) additions.
