# S3: MDL compression (PR #224)

## Byte counts

| | design (md) | code (touched files) |
|---|---:|---:|
| pre | ~700 | ~25000 |
| post | ~700 | ~28000 |

Non-comment Go LOC: +49.

## Predictability of code given design

**Pre.** Score: 3 / 5. Pre-design (the version that existed at the time) does not document the readiness propagation gap, the cluster-scoped routing mismatch, or the stale dispatch bug — these are bugs, so the design did not anticipate them. A regenerator following pre-design would produce code that misses these cases.

**Post.** Score: 4 / 5. Post-design (and the code that pairs with it) names the three cases. A regenerator produces handling for each. Predictability rises but at the cost of more design and code bytes.

## MDL comparison

`|design| + cost(code | design)` grew on both sides. The change adds three new structural commitments (readiness symmetry, cluster-scoped scope check, deferred dispatch). The design does not absorb any prose into structure that pre held without backing; it adds new commitments where pre had silence.

This is not an MDL compression move. It is a correctness fix that incidentally improves predictability. S3 should not score it as `improved` purely because predictability rose; the bytes grew and no compression occurred.

## Verdict

**S3 unchanged.** Code grew, design grew, predictability rose marginally. No compression of an existing description; new structure adds rather than absorbs.

Citation: +49 LOC, design length grows correspondingly, no MDL win on either side.
