# S2: partition match (PR #172)

## Input partition

- Node type values (template / patch / ref / watch / def).
- User-facing YAML keyword (template: / patch: / ref: / watch: / def:).
- Reader searches for a concept by name.

## Branching partition (pre)

- Node-type dispatch on `NodeType` enum: 5 cells.
- Public YAML keyword to type mapping: 5 cells.
- Internal vocabulary cells (comments, log fields, test helpers) use "Own"/"Contribute" for two of the five public types; reader must learn the mapping.

## Branching partition (post)

- Identical to pre at the type-dispatch and YAML-keyword level.
- Internal vocabulary cells use template/patch consistently; mapping is identity.

## Cells in pre's branching partition not in post's

- **Internal-vs-canonical vocabulary mapping cell.** Classification: **(b) deleted**. The cell collapsed because the distinction no longer exists. The input partition does not require this cell — readers don't need to distinguish "what the comment calls it" from "what the type calls it"; they want the canonical name.

## Cells in post's branching partition not in pre's

- None.

## Verdict

**S2 unchanged.** Branching partition is identical in shape. The mapping-cell that pre carried is not part of what S2 measures; S2 looks at whether the design's branches match input cases the preferences require, and the input partition here did not include "two-vocabulary mapping" as an input case.

S2 reports `unchanged` not `improved` because the partition shape was already correct in pre; only the labels on the cells changed. This is consistent with S1's orthogonal verdict.

Citation: identical 5-cell type dispatch in both designs; the only difference is the names on the cells.
