# S1: deletion-minimality (PR #261)

## Procedures and properties in design-pre absent from design-post

- **P4** (pre: `dag/` mixes compile-time and reconcile-time concepts). Classification: **(c) relocated to module structure**. The mixed cell is collapsed by moving `walkstate.go`; the reconcile-time concepts move to where they belong. Pre's negative-form property becomes post's affirmative property.
- **P5** (pre: import graph does not encode the split). Classification: **(c) relocated**. The import graph now encodes what the design says.
- **Cross-package imports of `dag/` from reconcile-time files** (architecture note). Classification: **(c) relocated to module structure**.

## Procedures and properties in design-post absent from design-pre

- None genuinely new. Every post-state property is the affirmative version of a pre-state property; the same input cases (compile-time vs reconcile-time, walk-state typing) are now handled by module structure rather than by package contents.

## Verdict

**S1 improved.** All three pre-state cells absent from post collapse via (c) relocation to module structure. No deletion of preference-rewarded units. No introduction of accidental complexity. Note: this PR walks back PR #220, which previously placed `walkstate.go` in `dag/`. Applied to the pre-#220 state, S1 would have flagged the same input cases that #261 corrects.

Citation: P4/P5 and architecture note all map to (c) module-structure relocations; no deletion of input cases.
