# C1: deletion-minimality (comparison)

Here are two designs of a related system, A and B, and the union (or intersection) of their test suites. Both pass all tests in the suite. Which has more removable units? Return -1 if B has fewer (B is more deletion-minimal), +1 if A has fewer, 0 if they are tied or comparable. Justify in one sentence.
